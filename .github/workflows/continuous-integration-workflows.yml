name: CI
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: Set up Go 1.12
        with:
          go-version: 1.12
        uses: actions/setup-go@v1
      - name: Build
        run: go build -v .
